<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.seckill.dao.SuccessKilledDao">
    <insert id="insertSuccessKilled">
        INSERT IGNORE INTO success_killed(seckill_id, product_id, user_phone, state, gmt_create, gmt_modified)
        VALUES (#{seckillId}, #{productId}, #{userPhone}, 0, NOW(), NOW())
    </insert>
    <select id="getSuccessKilledBySeckillId" resultType="org.seckill.domain.SuccessKilled">
        SELECT id, seckill_id, product_id, user_phone, state, gmt_create, gmt_modified
        FROM success_killed
        WHERE product_id = #{productId}
        AND user_phone = #{userPhone}
    </select>
</mapper>
        